{{- range $name, $glob := .Values.configMaps }}
{{- $files := $.Files.Glob $glob }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $name }}-config
  namespace: {{ $.Values.namespace }}
data:
  {{ $files.AsConfig | indent 2 }}
---
{{- end }}