apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  {{- if .Values.ports }}
  ports:
  {{- range .Values.ports }}
    - port: {{ . }}
      targetPort: {{ . }}
  {{- end }}
  {{- end }}
  
  selector:
    app: {{ .Values.name }}