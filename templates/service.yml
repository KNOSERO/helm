apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
  {{- range .Values.ports }}
    - port: {{ . }}
      targetPort: {{ . }}
  {{- end }}

  selector:
    app: {{ .Values.name }}