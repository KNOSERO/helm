name: test
namespace: test
replicas: 1

image:
  repository: hello-world
  tag: latest
  pullPolicy: always

service:
  type: ClusterIP

ports:
  - 8080

volumes:
  - name: workspace
    hostPath: "/mnt/workspace"
    mountPath: "/workspace"

ingress:
  hosts:
    - domain: test.local
      port: 8080